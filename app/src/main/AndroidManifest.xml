<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.kiyoonko.virms"
    android:versionCode="0"
    android:versionName="0">

    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="VirMS"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <uses-library
            android:name="com.gmail.libtango_device"
            android:required= "true" />
        <activity
            android:name="com.gmail.kiyoonko.virms.StartActivity"
            android:lable="Drawing in 3D Space"
            android:screenOrientation="landscape"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.gmail.kiyoonko.virms.MotionTrackingActivity"
            android:label="Project Tango Motion Tracking"
            android:screenOrentation="landscape">

        </activity>
    </application>

</manifest>
